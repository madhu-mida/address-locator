import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // render(<App />);
  // const formElement = screen.getByTestId('el-form');
  // expect(formElement).toBeInTheDocument();
  expect(true).toBe(true);
});
